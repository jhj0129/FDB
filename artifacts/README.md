# Demonstration artifacts

`fdb_final_pick_place.mp4` is the deterministic final Panda demonstration generated by
`python -m fdb.v2.render_final`. The red box starts at the source location, is grasped
and lifted, moves to the blue target zone, is released, and remains stable while the
robot retreats. Its machine-readable verification metrics and final frame are stored
beside the video.

